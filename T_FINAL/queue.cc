/*
#include <iostream>
#include <queue>

int main() {
    std::queue<int> fila;

    // Inserindo elementos na fila
    fila.push(10);
    fila.push(20);
    fila.push(30);

    // Verificando o tamanho da fila
    std::cout << "Tamanho da fila: " << fila.size() << std::endl;

    // Acessando o elemento da frente da fila
    std::cout << "Elemento da frente: " << fila.front() << std::endl;

    // Removendo o elemento da frente da fila
    fila.pop();

    // Verificando novamente o elemento da frente da fila
    std::cout << "Novo elemento da frente: " << fila.front() << std::endl;

    // Verificando se a fila está vazia
    if (fila.empty()) {
        std::cout << "A fila está vazia." << std::endl;
    } else {
        std::cout << "A fila não está vazia." << std::endl;
    }

    return 0;
}
*/